## চাকরির আবেদন মাল্টি-টুল (ক্লিপবোর্ড + রিসাইজার)
`এটি একটি পাইথন ভিত্তিক GUI (গ্রাফিক্যাল ইউজার ইন্টারফেস) অ্যাপ্লিকেশন। চাকরির আবেদন প্রক্রিয়া সহজ করার জন্য এই টুলটি তৈরি করা হয়েছে।`

## এই টুলের দুটি প্রধান কাজ: 
* ** ১. ক্লিপবোর্ড ম্যানেজার: `config.json` ফাইল থেকে আপনার সব ব্যক্তিগত তথ্য (যেমন নাম, ঠিকানা, শিক্ষাগত যোগ্যতা) লোড করে এবং এক ক্লিকে ক্লিপবোর্ডে কপি করার সুবিধা দেয়। 
* ** ২. ইমেজ রিসাইজার: টেলিটকের (Teletalk) চাকরির আবেদনে যেরকম নির্দিষ্ট মাপের ছবি `(300x300) ` এবং সিগনেচার `(300x80)` প্রয়োজন হয়, সেটি এক ক্লিকে তৈরি করে দেয়।

## 🎯 প্রধান ফিচারসমূহ (Features)
-- ট্যাবড ইন্টারফেস: সব তথ্য (Basic Info, Address, Education) এবং টুল (Resizer) আলাদা আলাদা ট্যাবে সাজানো।

* ** JSON থেকে ডেটা লোড: `config.json` ফাইল থেকে স্বয়ংক্রিয়ভাবে সব তথ্য লোড করে।
* ** ওয়ান-ক্লিক কপি: প্রতিটি তথ্যের পাশে "Copy" বাটন রয়েছে।
* ** এডিট ও সেভ: "Unlock to Edit" বাটনে ক্লিক করে তথ্য এডিট করা যায় এবং "Save Changes" বাটনে ক্লিক করে তা সরাসরি `config.json` ফাইলে সেভ করা যায়।
* ** ফটো রিসাইজার: যেকোনো ছবিকে টেলিটকের জন্য পারফেক্ট 300x300 পিক্সেল সাইজে রিসাইজ করে।
* ** সিগনেচার রিসাইজার: যেকোনো সিগনেচারকে 300x80 পিক্সেল সাইজে রিসাইজ করে।

## 🛠️ ক্লিপবোর্ড টুল:
* ** প্রথমে `Browse and Load config.json` বাটনে ক্লিক করে আপনার `config.json` ফাইলটি সিলেক্ট করুন।
* ** সব তথ্য স্বয়ংক্রিয়ভাবে লোড হয়ে যাবে।
* ** যেকোনো তথ্যের পাশের "Copy" বাটনে ক্লিক করে সেটি ক্লিপবোর্ডে কপি করুন।
* ** তথ্য পরিবর্তন করতে চাইলে, "Unlock to Edit" বাটনে ক্লিক করুন, তথ্য এডিট করুন, এবং "Save Changes" বাটনে ক্লিক করে ফাইলে সেভ করুন।
* ** ইমেজ রিসাইজার টুল:
* ** "Photo Resizer (300x300)" বা "Signature Resizer (300x80)" ট্যাবে যান।
* ** "Browse..." বাটনে ক্লিক করে আপনার ছবি বা সিগনেচার সিলেক্ট করুন।
* ** "Resize... & Save" বাটনে ক্লিক করুন।
* ** নতুন ফাইলটি কোথায় সেভ করতে চান তা দেখিয়ে দিন। আপনার কাজ শেষ!
